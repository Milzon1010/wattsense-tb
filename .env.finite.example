# --- Finite publisher settings ---
# Target broker (use ThingsBoard host or local Mosquitto)
MQTT_BROKER=127.0.0.1
MQTT_PORT=1883
# If sending to ThingsBoard, put the device token here; leave empty for plain Mosquitto
MQTT_ACCESS_TOKEN=
# Default ThingsBoard telemetry topic:
MQTT_TOPIC=v1/devices/me/telemetry

# Finite loop controls
SEND_COUNT=20
INTERVAL_SECONDS=5
# Where to save CSV log (auto creates folder if needed)
CSV_LOG_PATH=logs/mqtt_telemetry.csv
